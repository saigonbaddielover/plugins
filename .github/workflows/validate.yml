name: validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  catalog:
    runs-on: [self-hosted, Linux, X64, plugins-local]
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/validate.py --online
